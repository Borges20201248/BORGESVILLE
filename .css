@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Montserrat:wght@300;400;500;700&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background-color: #0d0b0a;
    background-image: radial-gradient(rgba(29, 24, 21, 0.8) 15%, transparent 15%),
                      radial-gradient(rgba(29, 24, 21, 0.8) 15%, transparent 15%);
    background-size: 4px 4px;
    background-position: 0 0, 2px 2px;
    color: #e5dac9;
    font-family: 'Montserrat', sans-serif;
    padding-top: 90px;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
}

.navbar {
    position: fixed;
    top: 0;
    width: 100%;
    height: 85px;
    background-color: rgba(18, 15, 13, 0.95);
    backdrop-filter: blur(10px);
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 6%;
    z-index: 1000;
    border-bottom: 1px solid rgba(198, 134, 66, 0.25);
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);
}

.header-left {
    display: flex;
    align-items: center;
    gap: 20px;
}

.brand-name {
    font-family: 'Cinzel', serif;
    font-size: 1.6rem;
    font-weight: 700;
    letter-spacing: 3px;
    color: #d49a5b;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.6);
}

.logo-placeholder {
    width: 90px;
    height: 40px;
    border: 1px solid rgba(198, 134, 66, 0.4);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.65rem;
    color: #d49a5b;
    letter-spacing: 1px;
    text-transform: uppercase;
    background: rgba(198, 134, 66, 0.05);
}

.nav-links a {
    color: #cbbcb0;
    text-decoration: none;
    margin: 0 20px;
    font-size: 0.8rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 2px;
    transition: all 0.3s ease;
    position: relative;
    padding-bottom: 5px;
}

.nav-links a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 0;
    height: 1px;
    background-color: #d49a5b;
    transition: all 0.3s ease;
    transform: translateX(-50%);
}

.nav-links a:hover {
    color: #d49a5b;
}

.nav-links a:hover::after {
    width: 80%;
}

.btn-login {
    background: transparent;
    color: #d49a5b;
    border: 1px solid #d49a5b;
    padding: 10px 26px;
    border-radius: 0px;
    text-decoration: none;
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.75rem;
    letter-spacing: 1px;
    transition: all 0.3s ease;
}

.btn-login:hover {
    background-color: #d49a5b;
    color: #120f0d;
    box-shadow: 0 0 15px rgba(212, 148, 91, 0.4);
}

.content {
    max-width: 1300px;
    margin: 50px auto;
    padding: 0 30px;
}

.titulo-pagina {
    text-align: center;
    margin-bottom: 60px;
    position: relative;
}

.titulo-pagina::before {
    content: '❖';
    color: #d49a5b;
    font-size: 1rem;
    display: block;
    margin-bottom: 10px;
    opacity: 0.7;
}

.titulo-pagina h2 {
    font-family: 'Cinzel', serif;
    font-size: 3rem;
    margin-bottom: 15px;
    letter-spacing: 2px;
    background: linear-gradient(to right, #ffffff, #d49a5b, #ffffff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.titulo-pagina p {
    color: #9c8e82;
    font-size: 1.05rem;
    font-style: italic;
    font-weight: 300;
}

.whisky-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 40px;
    margin-bottom: 60px;
}

.whisky-card {
    background: #171311;
    border: 1px solid rgba(198, 134, 66, 0.15);
    border-radius: 0px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    position: relative;
    transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
}

.whisky-card:hover {
    transform: translateY(-8px);
    border-color: rgba(212, 148, 91, 0.6);
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.7), 
                0 0 20px rgba(212, 148, 91, 0.1);
}

.whisky-image-wrapper {
    width: 100%;
    aspect-ratio: 1 / 1; 
    background: radial-gradient(circle, #261f1b 0%, #120f0d 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-bottom: 1px solid rgba(198, 134, 66, 0.15);
    position: relative;
}

.whisky-image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s cubic-bezier(0.25, 0.8, 0.25, 1);
}

.whisky-card:hover .whisky-image-wrapper img {
    transform: scale(1.06);
}

.whisky-details {
    padding: 25px;
    display: flex;
    flex-direction: column;
    gap: 15px;
    text-align: center;
    flex-grow: 1;
    justify-content: space-between;
    background-color: #161210;
}

.whisky-title {
    font-family: 'Cinzel', serif;
    font-size: 1.25rem;
    color: #e5dac9;
    font-weight: 600;
    letter-spacing: 1px;
    min-height: 2.6rem;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    transition: color 0.3s;
}

.whisky-card:hover .whisky-title {
    color: #d49a5b;
}

.whisky-price-tag {
    font-family: 'Montserrat', sans-serif;
    font-size: 1.5rem;
    color: #d49a5b;
    font-weight: 700;
    letter-spacing: 1px;
}

.btn-detalhes {
    background: #1c1714;
    color: #d49a5b;
    border: 1px solid rgba(212, 148, 91, 0.4);
    padding: 14px;
    text-decoration: none;
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    border-radius: 0px;
    transition: all 0.3s ease;
}

.btn-detalhes:hover {
    background-color: #d49a5b;
    color: #120f0d;
    border-color: #d49a5b;
    box-shadow: 0 4px 15px rgba(212, 148, 91, 0.2);
}


.carousel-container {
    width: 100%;
    overflow: hidden;
    margin: 40px auto;
    padding: 20px 0;
}

.carousel-track {
    display: flex;
    gap: 20px;
    width: max-content;
    animation: scroll 25s linear infinite;
}

.carousel-track:hover {
    animation-play-state: paused;
}

.product-card-carousel {
    width: 250px;
    height: 250px;
    background: #171311;
    border: 1px solid rgba(198, 134, 66, 0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.product-card-carousel img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
}

.product-card-carousel:hover img {
    transform: scale(1.05);
}

@keyframes scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(calc(-270px * 2)); }
}

@media (max-width: 768px) {
    .navbar {
        padding: 0 4%;
    }
    .brand-name {
        font-size: 1.3rem;
    }
    .nav-links {
        display: none; 
    }
    .titulo-pagina h2 {
        font-size: 2.2rem;
    }
}